# Single-node local cluster. kind's default CNI doesn't enforce NetworkPolicy, so it's
# disabled and `make cluster` installs Cilium (which also gives us FQDN egress rules).
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  disableDefaultCNI: true
nodes:
  - role: control-plane
